var fs = require('fs');
var path = require('path');

function run(req , res , next)
{
 /*   if ( fs.existsSync('./vendor/autoload.php') )
    {
        console.log('./vendor/autoload.php => exist');
    }
    else
    {   
        console.log('./vendor/autoload.php => not exist');
        res.redirect('setup/composertest.html');
    }*/
}

module.exports.run = run;